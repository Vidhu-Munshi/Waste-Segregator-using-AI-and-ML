# Waste-Segrator-using-Ai-and-Ml
This project implements a deep learning-based waste segregation system using a Convolutional Neural Network (CNN). The model is trained to classify common waste categories and achieves approximately 80% classification accuracy.
